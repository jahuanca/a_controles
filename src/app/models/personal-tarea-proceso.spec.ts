import { PersonalTareaProceso } from './personal-tarea-proceso';

describe('PersonalTareaProceso', () => {
  it('should create an instance', () => {
    expect(new PersonalTareaProceso()).toBeTruthy();
  });
});

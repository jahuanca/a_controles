import { PreTareoProcesoUvaDetalle } from './pre-tareo-proceso-uva-detalle';

describe('PreTareoProcesoUvaDetalle', () => {
  it('should create an instance', () => {
    expect(new PreTareoProcesoUvaDetalle()).toBeTruthy();
  });
});
